{
  "name": "grigliepromo-ai-proxy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Proxy backend that holds the Anthropic API key and serves AI promo-grid suggestions to the frontend.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "zod": "^3.25.0"
  }
}
